# Valentine's Day Project

## Development Workflow

- After making any code changes, always run `npm run build` to verify the build succeeds before telling the user to run `npm run dev`
- Continuously commit and push changes after verifying the build works

## Tech Stack

- Next.js 16 with App Router
- TypeScript
- Tailwind CSS
- shadcn/ui components
